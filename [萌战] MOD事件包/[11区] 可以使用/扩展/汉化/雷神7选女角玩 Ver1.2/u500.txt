eve_start
	if_val,性別:player=1
	//if_chksts,player,年齢,>15
eve_end

set_epid1,player
set_epid3,1098
//這裡直接用ID
//668,1098

//set_epid2,0
//set_epid4,0


chk_mval,状態:pid1=5,,,,,,
chk_mval_then
//這裡用條件選
	gosub,*choosewoman

	set_dead,pid1,0
	chg_player,pid3,0
	set_epid1,player
	set_virgin,pid1

	make_name,pid1

	chk_sts,pid1,2,>18
	chk_sts_then
		set_pnen,pid1,18
	chk_sts_else

	chk_sts,pid1,2,<16
	chk_sts_then
		set_pnen,pid1,16
	chk_sts_else

	chk_ormval,状態:pid1=3,状態:pid1>5,状態:pid1=4,,
	chk_ormval_then
		chk_msts,pid1,状態,3,,,,,,,,
		chk_msts_then
			set_eveflg,250,0
			for,PMAX,250,1
				chk_family,pid1,eveflg250
				case_fam_母
					upd_sts,eveflg250,母,0
					continue
				default_fam
					continue
			next
		chk_msts_else
			set_hourou,pid1
			upd_sts,pid1,状態,5
	chk_ormval_else

	chk_sts,pid1,知力,<50
	chk_sts_then
		upd_sts,pid1,知力,-50
		upd_sts,pid1,知力,50
	chk_sts_else

	chk_sts,pid1,攻撃,<50
	chk_sts_then
		upd_sts,pid1,攻撃,-50
		upd_sts,pid1,攻撃,50
	chk_sts_else

	chk_sts,pid1,防御,<50
	chk_sts_then
		upd_sts,pid1,防御,-50
		upd_sts,pid1,防御,50
	chk_sts_else

	chk_sts,pid1,格闘,<50
	chk_sts_then
		upd_sts,pid1,格闘,-50
		upd_sts,pid1,格闘,50
	chk_sts_else

	chk_sts,pid1,交渉,<50
	chk_sts_then
		upd_sts,pid1,交渉,-50
		upd_sts,pid1,交渉,50
	chk_sts_else

	upd_sts,pid1,知力,20
	upd_sts,pid1,攻撃,20
	upd_sts,pid1,防御,20
	upd_sts,pid1,格闘,20
	upd_sts,pid1,交渉,20

	chk_msts,pid1,勲功,13000,状態,2,,,,,,
	chk_msts_then
		goto,*doking
	chk_msts_else
		goto,*doplay
	end

chk_mval_else

end


*doking
set_eveflg,250,0
for,10,250,0
	add_sdev,poid_player,eveflg250,1000
next
get_item,pid1,,一族の証
get_item,pid1,,一族の証
get_item,pid1,,一族の証
goto,*additem

*doplay
get_item,pid1,,一族の家宝
get_item,pid1,,一族の家宝
get_item,pid1,,一族の家宝

*additem
get_item,pid1,,飛行戦術書
get_item,pid1,,奇策 分裂波動砲
get_item,pid1,,奇策 波動砲
get_item,pid1,,特別部隊招集
get_item,pid1,,全身性感香
get_item,pid1,,精力剤
get_item,pid1,,税務署の心得
get_item,pid1,,カリスマの書

//drop_item,9,媚薬
get_item,pid1,,媚薬

end


//選擇女性
*choosewoman
get_mpid,状態=4|16|19,性別=2,,,,,
//get_mpid,勲功=12100,性別=2,状態!3,年齢>3,,,
//get_mpid,勲功=13000,性別=2,状態=2,,,,
//get_mpid,勲功=12200,性別=2,,,,,
set_epid3,pval
zin_reg_face_1,pid3
zin_reg_msg,選擇要扮演的女性,1
zin_reg_2sel,就選他,選其它人
get_resp
ret_sel1
	zin_reg_msg,就這個！,0
	return
ret_sel2
	txtload,u500
